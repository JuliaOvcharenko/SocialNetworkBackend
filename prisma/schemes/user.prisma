model User {
    id        Int      @id @default(autoincrement())
    name      String
    surname   String
    nickname  String   @unique
    email     String   @unique
    birthDate DateTime @updatedAt
    password  String
    signature String
}
